# StyleTTS2 Railway API

### Endpoints:
GET / → Check status  
GET /tts?text=Hello → Generate audio

### Deploy on Railway:
1. Create New → Deploy from Repo  
2. Add environment:
   PORT = 8000
3. Deploy
